---
interface Props {
	title?: string;
}

const { title } = Astro.props;
---

<section class="relative w-full max-w-2xl space-y-6">
	{
		title && (
			<h3 class="flex items-center text-xl gap-3">
				<span class="w-2 h-2 bg-primary rounded-full" />
				{title}
			</h3>
		)
	}
	<slot />
</section>
